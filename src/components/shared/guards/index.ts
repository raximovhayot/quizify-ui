// Export all guard components for easy importing
export { default as GuardAuthenticated } from '../GuardAuthenticated';
export { default as GuardPublicOnly } from '../GuardPublicOnly';

// Re-export types for convenience
export type { UserRole } from '@/components/features/profile/types/account';
