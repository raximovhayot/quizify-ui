# Environment Variables for Quizify UI

# ============================================================================
# REQUIRED FOR BUILD AND RUNTIME
# ============================================================================

# NextAuth.js Configuration
# Generate a secure secret with: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-key-here-minimum-32-characters-long

# NextAuth.js URL (usually auto-detected, but can be set explicitly)
# NEXTAUTH_URL=http://localhost:3000

# Backend API Configuration
# The base URL for the Quizify backend API
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api

# ============================================================================
# OPTIONAL - ENVIRONMENT INDICATORS
# ============================================================================

# Environment Display Name (shown in UI)
# NEXT_PUBLIC_ENV_NAME=LOCAL

# Environment Emoji (shown in UI)
# NEXT_PUBLIC_ENV_EMOJI=🚧

# Environment Color Scheme (used for visual indicators)
# Options: 'warning', 'success', 'info', 'error'
# NEXT_PUBLIC_ENV_COLOR_SCHEME=warning

# ============================================================================
# DEVELOPMENT ONLY
# ============================================================================

# Skip environment variable validation (useful for Docker/CI builds)
# SKIP_ENV_VALIDATION=true

# ============================================================================
# NOTES
# ============================================================================
#
# 1. Copy this file to .env.local for local development
#    cp .env.example .env.local
#
# 2. Never commit .env.local or any file containing real secrets to git
#
# 3. For production deployment, set these environment variables in your
#    hosting platform (Vercel, Netlify, etc.)
#
# 4. The NEXT_PUBLIC_* variables are exposed to the browser, so never
#    put sensitive information in them
#
# 5. For CI/CD builds where you don't need actual values, you can use:
#    SKIP_ENV_VALIDATION=true npm run build
#
